


<%= simple_form_for(@post) do |f| %>
  


  
    <%= f.error_notification %>

  <div class="field">
    <div class="control">
    <%= f.input :body, label: "Post about it", input_html: { class: "text_area" }, 
    wrapper: false, label_html: {class: "label"}, placeholder: "Compose a new post...", autofocus: true %>
    </div>
  </div>
  <%= f.button :submit, class: "button-is-info" %>
<% end %>

























<% if false %>


<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

<% if false %>
  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>
<% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<% end %>














